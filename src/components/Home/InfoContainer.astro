---
import { WebsiteConfig } from "../../config"
---

<div class="flex md:flex-row flex-col py-8 bg-base-200">
  <div class="main-container flex flex-col md:flex-row w-full m-auto px-4">
    <div class="flex-1 md:w-64 flex flex-col justify-center text-center">
      <p class="text-5xl font-bold mb-4">What is Devfest Bari?</p>
      <p class="text-lg">
        <b> DevFest Bari</b>, or ‘Developers Festival’, is one of the local
        conferences hosted by Google Developer Groups (GDG) around the world.
        Each DevFest is crafted by its local organizers to fit the needs and
        interests of its local developer community. Whether it be through
        hands-on learning experiences, technical talks delivered in local
        languages by experts, or by simply meeting fellow local developers,
        DevFest attendees learn how to build together and innovate on Google's
        developer tools.
      </p>
    </div>
    <div class="flex-1 grid grid-cols-2 grid-rows-2 gap-10">
      {[
        ["Participants", "community", WebsiteConfig.NUM_PARTECIPENTS],
        ["Speakers", "speakers", WebsiteConfig.NUM_SPEAKERS],
        ["Categories", "categories", WebsiteConfig.NUM_TALK_CATEGORIES],
        ["Tracks", "tracks", WebsiteConfig.NUM_TRACKS]
    ].map( ([name, icon, num]:[string, string, number]) => {
        return <div class="flex text-xl items-center justify-center flex-col">
            <img src={`/assets/vectors/${icon}.svg`} class="text-red fill-current pb-5 sm:w-28 sm:h-28 w-44 h-44" />
            <h2 class="font-bold text-5xl">{num}</h2>
            <span class="text-xl underline">{name}</span>
        </div>
    })}
    </div>
  </div>
</div>
